"use client";

// Legacy file kept to preserve import paths.
// The canonical subscription context is Supabase-based in `src/context/SubscriptionProvider.tsx`.
export { SubscriptionProvider, useSubscription, useSubscriptionContext } from '@/context/SubscriptionProvider';